#!/bin/sh

set -e

export LINTIAN_TEST_INSTALLED=yes
WORKDIR="$AUTOPKGTEST_TMP/$(basename $0)"

bin/build-test-packages --debug --work-dir="$WORKDIR"
bin/runtests --debug --unattended --keep-going --work-dir="$WORKDIR" --onlyrun="suite:recipes"

rm -rf "${WORKDIR}"
